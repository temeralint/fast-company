import { useLocation, useParams } from "react-router-dom";
import query from 'query-string';

const User = () => {
    return (
        <div>
            one user
        </div>
    );
};

export default User;